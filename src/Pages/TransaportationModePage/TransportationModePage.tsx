import Layout from '../../components/Layout/index'
import TransportationModeComp from '../../components/TransportationMode';

export default function TransportationModePage() {
    return (
        <Layout>
            <TransportationModeComp />
        </Layout>
    );
}